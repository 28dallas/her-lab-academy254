# Her Lab Academy — Implementation Status

## Push completed
- [x] Push local work to `28dallas/her-lab-academy254`.

## Next: Add courses + enrollment code support
- [x] Update `src/lib/courseEnrollmentPrefixes.ts` to include the new course titles/prefixes.
- [x] Add UI assets for each course card (emoji/cover image per course).
- [x] Ensure search + course suggestions in `/courses` include these titles (depends on publishing + filtering by title/category).
- [x] Ensure admin/teacher can assign enrollment codes matching the requested format (prefix + 5 digits).
- [x] Commit + push changes.
