# Her Lab Academy — Implementation Status

## Push completed
- [x] Push local work to `28dallas/her-lab-academy254`.

## Next: Add courses + enrollment code support
- [ ] Update `src/lib/courseEnrollmentPrefixes.ts` to include the new course titles/prefixes.
- [ ] Add UI assets for each course card (emoji/cover image per course).
- [ ] Ensure search + course suggestions in `/courses` include these titles (depends on publishing + filtering by title/category).
- [ ] Ensure admin/teacher can assign enrollment codes matching the requested format (prefix + 5 digits).
- [ ] Commit + push changes.
